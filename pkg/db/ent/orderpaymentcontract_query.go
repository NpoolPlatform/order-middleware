// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/order-middleware/pkg/db/ent/orderpaymentcontract"
	"github.com/NpoolPlatform/order-middleware/pkg/db/ent/predicate"
)

// OrderPaymentContractQuery is the builder for querying OrderPaymentContract entities.
type OrderPaymentContractQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.OrderPaymentContract
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the OrderPaymentContractQuery builder.
func (opcq *OrderPaymentContractQuery) Where(ps ...predicate.OrderPaymentContract) *OrderPaymentContractQuery {
	opcq.predicates = append(opcq.predicates, ps...)
	return opcq
}

// Limit adds a limit step to the query.
func (opcq *OrderPaymentContractQuery) Limit(limit int) *OrderPaymentContractQuery {
	opcq.limit = &limit
	return opcq
}

// Offset adds an offset step to the query.
func (opcq *OrderPaymentContractQuery) Offset(offset int) *OrderPaymentContractQuery {
	opcq.offset = &offset
	return opcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (opcq *OrderPaymentContractQuery) Unique(unique bool) *OrderPaymentContractQuery {
	opcq.unique = &unique
	return opcq
}

// Order adds an order step to the query.
func (opcq *OrderPaymentContractQuery) Order(o ...OrderFunc) *OrderPaymentContractQuery {
	opcq.order = append(opcq.order, o...)
	return opcq
}

// First returns the first OrderPaymentContract entity from the query.
// Returns a *NotFoundError when no OrderPaymentContract was found.
func (opcq *OrderPaymentContractQuery) First(ctx context.Context) (*OrderPaymentContract, error) {
	nodes, err := opcq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{orderpaymentcontract.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (opcq *OrderPaymentContractQuery) FirstX(ctx context.Context) *OrderPaymentContract {
	node, err := opcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first OrderPaymentContract ID from the query.
// Returns a *NotFoundError when no OrderPaymentContract ID was found.
func (opcq *OrderPaymentContractQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = opcq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{orderpaymentcontract.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (opcq *OrderPaymentContractQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := opcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single OrderPaymentContract entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one OrderPaymentContract entity is found.
// Returns a *NotFoundError when no OrderPaymentContract entities are found.
func (opcq *OrderPaymentContractQuery) Only(ctx context.Context) (*OrderPaymentContract, error) {
	nodes, err := opcq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{orderpaymentcontract.Label}
	default:
		return nil, &NotSingularError{orderpaymentcontract.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (opcq *OrderPaymentContractQuery) OnlyX(ctx context.Context) *OrderPaymentContract {
	node, err := opcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only OrderPaymentContract ID in the query.
// Returns a *NotSingularError when more than one OrderPaymentContract ID is found.
// Returns a *NotFoundError when no entities are found.
func (opcq *OrderPaymentContractQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = opcq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{orderpaymentcontract.Label}
	default:
		err = &NotSingularError{orderpaymentcontract.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (opcq *OrderPaymentContractQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := opcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of OrderPaymentContracts.
func (opcq *OrderPaymentContractQuery) All(ctx context.Context) ([]*OrderPaymentContract, error) {
	if err := opcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return opcq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (opcq *OrderPaymentContractQuery) AllX(ctx context.Context) []*OrderPaymentContract {
	nodes, err := opcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of OrderPaymentContract IDs.
func (opcq *OrderPaymentContractQuery) IDs(ctx context.Context) ([]uint32, error) {
	var ids []uint32
	if err := opcq.Select(orderpaymentcontract.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (opcq *OrderPaymentContractQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := opcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (opcq *OrderPaymentContractQuery) Count(ctx context.Context) (int, error) {
	if err := opcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return opcq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (opcq *OrderPaymentContractQuery) CountX(ctx context.Context) int {
	count, err := opcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (opcq *OrderPaymentContractQuery) Exist(ctx context.Context) (bool, error) {
	if err := opcq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return opcq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (opcq *OrderPaymentContractQuery) ExistX(ctx context.Context) bool {
	exist, err := opcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the OrderPaymentContractQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (opcq *OrderPaymentContractQuery) Clone() *OrderPaymentContractQuery {
	if opcq == nil {
		return nil
	}
	return &OrderPaymentContractQuery{
		config:     opcq.config,
		limit:      opcq.limit,
		offset:     opcq.offset,
		order:      append([]OrderFunc{}, opcq.order...),
		predicates: append([]predicate.OrderPaymentContract{}, opcq.predicates...),
		// clone intermediate query.
		sql:    opcq.sql.Clone(),
		path:   opcq.path,
		unique: opcq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.OrderPaymentContract.Query().
//		GroupBy(orderpaymentcontract.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (opcq *OrderPaymentContractQuery) GroupBy(field string, fields ...string) *OrderPaymentContractGroupBy {
	grbuild := &OrderPaymentContractGroupBy{config: opcq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := opcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return opcq.sqlQuery(ctx), nil
	}
	grbuild.label = orderpaymentcontract.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.OrderPaymentContract.Query().
//		Select(orderpaymentcontract.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (opcq *OrderPaymentContractQuery) Select(fields ...string) *OrderPaymentContractSelect {
	opcq.fields = append(opcq.fields, fields...)
	selbuild := &OrderPaymentContractSelect{OrderPaymentContractQuery: opcq}
	selbuild.label = orderpaymentcontract.Label
	selbuild.flds, selbuild.scan = &opcq.fields, selbuild.Scan
	return selbuild
}

func (opcq *OrderPaymentContractQuery) prepareQuery(ctx context.Context) error {
	for _, f := range opcq.fields {
		if !orderpaymentcontract.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if opcq.path != nil {
		prev, err := opcq.path(ctx)
		if err != nil {
			return err
		}
		opcq.sql = prev
	}
	if orderpaymentcontract.Policy == nil {
		return errors.New("ent: uninitialized orderpaymentcontract.Policy (forgotten import ent/runtime?)")
	}
	if err := orderpaymentcontract.Policy.EvalQuery(ctx, opcq); err != nil {
		return err
	}
	return nil
}

func (opcq *OrderPaymentContractQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*OrderPaymentContract, error) {
	var (
		nodes = []*OrderPaymentContract{}
		_spec = opcq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*OrderPaymentContract).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &OrderPaymentContract{config: opcq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(opcq.modifiers) > 0 {
		_spec.Modifiers = opcq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, opcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (opcq *OrderPaymentContractQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := opcq.querySpec()
	if len(opcq.modifiers) > 0 {
		_spec.Modifiers = opcq.modifiers
	}
	_spec.Node.Columns = opcq.fields
	if len(opcq.fields) > 0 {
		_spec.Unique = opcq.unique != nil && *opcq.unique
	}
	return sqlgraph.CountNodes(ctx, opcq.driver, _spec)
}

func (opcq *OrderPaymentContractQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := opcq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (opcq *OrderPaymentContractQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   orderpaymentcontract.Table,
			Columns: orderpaymentcontract.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint32,
				Column: orderpaymentcontract.FieldID,
			},
		},
		From:   opcq.sql,
		Unique: true,
	}
	if unique := opcq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := opcq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, orderpaymentcontract.FieldID)
		for i := range fields {
			if fields[i] != orderpaymentcontract.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := opcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := opcq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := opcq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := opcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (opcq *OrderPaymentContractQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(opcq.driver.Dialect())
	t1 := builder.Table(orderpaymentcontract.Table)
	columns := opcq.fields
	if len(columns) == 0 {
		columns = orderpaymentcontract.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if opcq.sql != nil {
		selector = opcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if opcq.unique != nil && *opcq.unique {
		selector.Distinct()
	}
	for _, m := range opcq.modifiers {
		m(selector)
	}
	for _, p := range opcq.predicates {
		p(selector)
	}
	for _, p := range opcq.order {
		p(selector)
	}
	if offset := opcq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := opcq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (opcq *OrderPaymentContractQuery) ForUpdate(opts ...sql.LockOption) *OrderPaymentContractQuery {
	if opcq.driver.Dialect() == dialect.Postgres {
		opcq.Unique(false)
	}
	opcq.modifiers = append(opcq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return opcq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (opcq *OrderPaymentContractQuery) ForShare(opts ...sql.LockOption) *OrderPaymentContractQuery {
	if opcq.driver.Dialect() == dialect.Postgres {
		opcq.Unique(false)
	}
	opcq.modifiers = append(opcq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return opcq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (opcq *OrderPaymentContractQuery) Modify(modifiers ...func(s *sql.Selector)) *OrderPaymentContractSelect {
	opcq.modifiers = append(opcq.modifiers, modifiers...)
	return opcq.Select()
}

// OrderPaymentContractGroupBy is the group-by builder for OrderPaymentContract entities.
type OrderPaymentContractGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (opcgb *OrderPaymentContractGroupBy) Aggregate(fns ...AggregateFunc) *OrderPaymentContractGroupBy {
	opcgb.fns = append(opcgb.fns, fns...)
	return opcgb
}

// Scan applies the group-by query and scans the result into the given value.
func (opcgb *OrderPaymentContractGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := opcgb.path(ctx)
	if err != nil {
		return err
	}
	opcgb.sql = query
	return opcgb.sqlScan(ctx, v)
}

func (opcgb *OrderPaymentContractGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range opcgb.fields {
		if !orderpaymentcontract.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := opcgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := opcgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (opcgb *OrderPaymentContractGroupBy) sqlQuery() *sql.Selector {
	selector := opcgb.sql.Select()
	aggregation := make([]string, 0, len(opcgb.fns))
	for _, fn := range opcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(opcgb.fields)+len(opcgb.fns))
		for _, f := range opcgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(opcgb.fields...)...)
}

// OrderPaymentContractSelect is the builder for selecting fields of OrderPaymentContract entities.
type OrderPaymentContractSelect struct {
	*OrderPaymentContractQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (opcs *OrderPaymentContractSelect) Scan(ctx context.Context, v interface{}) error {
	if err := opcs.prepareQuery(ctx); err != nil {
		return err
	}
	opcs.sql = opcs.OrderPaymentContractQuery.sqlQuery(ctx)
	return opcs.sqlScan(ctx, v)
}

func (opcs *OrderPaymentContractSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := opcs.sql.Query()
	if err := opcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (opcs *OrderPaymentContractSelect) Modify(modifiers ...func(s *sql.Selector)) *OrderPaymentContractSelect {
	opcs.modifiers = append(opcs.modifiers, modifiers...)
	return opcs
}
