// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NpoolPlatform/order-middleware/pkg/db/ent/orderpaymenttransfer"
	"github.com/NpoolPlatform/order-middleware/pkg/db/ent/predicate"
)

// OrderPaymentTransferQuery is the builder for querying OrderPaymentTransfer entities.
type OrderPaymentTransferQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.OrderPaymentTransfer
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the OrderPaymentTransferQuery builder.
func (optq *OrderPaymentTransferQuery) Where(ps ...predicate.OrderPaymentTransfer) *OrderPaymentTransferQuery {
	optq.predicates = append(optq.predicates, ps...)
	return optq
}

// Limit adds a limit step to the query.
func (optq *OrderPaymentTransferQuery) Limit(limit int) *OrderPaymentTransferQuery {
	optq.limit = &limit
	return optq
}

// Offset adds an offset step to the query.
func (optq *OrderPaymentTransferQuery) Offset(offset int) *OrderPaymentTransferQuery {
	optq.offset = &offset
	return optq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (optq *OrderPaymentTransferQuery) Unique(unique bool) *OrderPaymentTransferQuery {
	optq.unique = &unique
	return optq
}

// Order adds an order step to the query.
func (optq *OrderPaymentTransferQuery) Order(o ...OrderFunc) *OrderPaymentTransferQuery {
	optq.order = append(optq.order, o...)
	return optq
}

// First returns the first OrderPaymentTransfer entity from the query.
// Returns a *NotFoundError when no OrderPaymentTransfer was found.
func (optq *OrderPaymentTransferQuery) First(ctx context.Context) (*OrderPaymentTransfer, error) {
	nodes, err := optq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{orderpaymenttransfer.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (optq *OrderPaymentTransferQuery) FirstX(ctx context.Context) *OrderPaymentTransfer {
	node, err := optq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first OrderPaymentTransfer ID from the query.
// Returns a *NotFoundError when no OrderPaymentTransfer ID was found.
func (optq *OrderPaymentTransferQuery) FirstID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = optq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{orderpaymenttransfer.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (optq *OrderPaymentTransferQuery) FirstIDX(ctx context.Context) uint32 {
	id, err := optq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single OrderPaymentTransfer entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one OrderPaymentTransfer entity is found.
// Returns a *NotFoundError when no OrderPaymentTransfer entities are found.
func (optq *OrderPaymentTransferQuery) Only(ctx context.Context) (*OrderPaymentTransfer, error) {
	nodes, err := optq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{orderpaymenttransfer.Label}
	default:
		return nil, &NotSingularError{orderpaymenttransfer.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (optq *OrderPaymentTransferQuery) OnlyX(ctx context.Context) *OrderPaymentTransfer {
	node, err := optq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only OrderPaymentTransfer ID in the query.
// Returns a *NotSingularError when more than one OrderPaymentTransfer ID is found.
// Returns a *NotFoundError when no entities are found.
func (optq *OrderPaymentTransferQuery) OnlyID(ctx context.Context) (id uint32, err error) {
	var ids []uint32
	if ids, err = optq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{orderpaymenttransfer.Label}
	default:
		err = &NotSingularError{orderpaymenttransfer.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (optq *OrderPaymentTransferQuery) OnlyIDX(ctx context.Context) uint32 {
	id, err := optq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of OrderPaymentTransfers.
func (optq *OrderPaymentTransferQuery) All(ctx context.Context) ([]*OrderPaymentTransfer, error) {
	if err := optq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return optq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (optq *OrderPaymentTransferQuery) AllX(ctx context.Context) []*OrderPaymentTransfer {
	nodes, err := optq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of OrderPaymentTransfer IDs.
func (optq *OrderPaymentTransferQuery) IDs(ctx context.Context) ([]uint32, error) {
	var ids []uint32
	if err := optq.Select(orderpaymenttransfer.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (optq *OrderPaymentTransferQuery) IDsX(ctx context.Context) []uint32 {
	ids, err := optq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (optq *OrderPaymentTransferQuery) Count(ctx context.Context) (int, error) {
	if err := optq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return optq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (optq *OrderPaymentTransferQuery) CountX(ctx context.Context) int {
	count, err := optq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (optq *OrderPaymentTransferQuery) Exist(ctx context.Context) (bool, error) {
	if err := optq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return optq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (optq *OrderPaymentTransferQuery) ExistX(ctx context.Context) bool {
	exist, err := optq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the OrderPaymentTransferQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (optq *OrderPaymentTransferQuery) Clone() *OrderPaymentTransferQuery {
	if optq == nil {
		return nil
	}
	return &OrderPaymentTransferQuery{
		config:     optq.config,
		limit:      optq.limit,
		offset:     optq.offset,
		order:      append([]OrderFunc{}, optq.order...),
		predicates: append([]predicate.OrderPaymentTransfer{}, optq.predicates...),
		// clone intermediate query.
		sql:    optq.sql.Clone(),
		path:   optq.path,
		unique: optq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.OrderPaymentTransfer.Query().
//		GroupBy(orderpaymenttransfer.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (optq *OrderPaymentTransferQuery) GroupBy(field string, fields ...string) *OrderPaymentTransferGroupBy {
	grbuild := &OrderPaymentTransferGroupBy{config: optq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := optq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return optq.sqlQuery(ctx), nil
	}
	grbuild.label = orderpaymenttransfer.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt uint32 `json:"created_at,omitempty"`
//	}
//
//	client.OrderPaymentTransfer.Query().
//		Select(orderpaymenttransfer.FieldCreatedAt).
//		Scan(ctx, &v)
//
func (optq *OrderPaymentTransferQuery) Select(fields ...string) *OrderPaymentTransferSelect {
	optq.fields = append(optq.fields, fields...)
	selbuild := &OrderPaymentTransferSelect{OrderPaymentTransferQuery: optq}
	selbuild.label = orderpaymenttransfer.Label
	selbuild.flds, selbuild.scan = &optq.fields, selbuild.Scan
	return selbuild
}

func (optq *OrderPaymentTransferQuery) prepareQuery(ctx context.Context) error {
	for _, f := range optq.fields {
		if !orderpaymenttransfer.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if optq.path != nil {
		prev, err := optq.path(ctx)
		if err != nil {
			return err
		}
		optq.sql = prev
	}
	if orderpaymenttransfer.Policy == nil {
		return errors.New("ent: uninitialized orderpaymenttransfer.Policy (forgotten import ent/runtime?)")
	}
	if err := orderpaymenttransfer.Policy.EvalQuery(ctx, optq); err != nil {
		return err
	}
	return nil
}

func (optq *OrderPaymentTransferQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*OrderPaymentTransfer, error) {
	var (
		nodes = []*OrderPaymentTransfer{}
		_spec = optq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*OrderPaymentTransfer).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &OrderPaymentTransfer{config: optq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(optq.modifiers) > 0 {
		_spec.Modifiers = optq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, optq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (optq *OrderPaymentTransferQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := optq.querySpec()
	if len(optq.modifiers) > 0 {
		_spec.Modifiers = optq.modifiers
	}
	_spec.Node.Columns = optq.fields
	if len(optq.fields) > 0 {
		_spec.Unique = optq.unique != nil && *optq.unique
	}
	return sqlgraph.CountNodes(ctx, optq.driver, _spec)
}

func (optq *OrderPaymentTransferQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := optq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (optq *OrderPaymentTransferQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   orderpaymenttransfer.Table,
			Columns: orderpaymenttransfer.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUint32,
				Column: orderpaymenttransfer.FieldID,
			},
		},
		From:   optq.sql,
		Unique: true,
	}
	if unique := optq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := optq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, orderpaymenttransfer.FieldID)
		for i := range fields {
			if fields[i] != orderpaymenttransfer.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := optq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := optq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := optq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := optq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (optq *OrderPaymentTransferQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(optq.driver.Dialect())
	t1 := builder.Table(orderpaymenttransfer.Table)
	columns := optq.fields
	if len(columns) == 0 {
		columns = orderpaymenttransfer.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if optq.sql != nil {
		selector = optq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if optq.unique != nil && *optq.unique {
		selector.Distinct()
	}
	for _, m := range optq.modifiers {
		m(selector)
	}
	for _, p := range optq.predicates {
		p(selector)
	}
	for _, p := range optq.order {
		p(selector)
	}
	if offset := optq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := optq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (optq *OrderPaymentTransferQuery) ForUpdate(opts ...sql.LockOption) *OrderPaymentTransferQuery {
	if optq.driver.Dialect() == dialect.Postgres {
		optq.Unique(false)
	}
	optq.modifiers = append(optq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return optq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (optq *OrderPaymentTransferQuery) ForShare(opts ...sql.LockOption) *OrderPaymentTransferQuery {
	if optq.driver.Dialect() == dialect.Postgres {
		optq.Unique(false)
	}
	optq.modifiers = append(optq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return optq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (optq *OrderPaymentTransferQuery) Modify(modifiers ...func(s *sql.Selector)) *OrderPaymentTransferSelect {
	optq.modifiers = append(optq.modifiers, modifiers...)
	return optq.Select()
}

// OrderPaymentTransferGroupBy is the group-by builder for OrderPaymentTransfer entities.
type OrderPaymentTransferGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (optgb *OrderPaymentTransferGroupBy) Aggregate(fns ...AggregateFunc) *OrderPaymentTransferGroupBy {
	optgb.fns = append(optgb.fns, fns...)
	return optgb
}

// Scan applies the group-by query and scans the result into the given value.
func (optgb *OrderPaymentTransferGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := optgb.path(ctx)
	if err != nil {
		return err
	}
	optgb.sql = query
	return optgb.sqlScan(ctx, v)
}

func (optgb *OrderPaymentTransferGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range optgb.fields {
		if !orderpaymenttransfer.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := optgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := optgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (optgb *OrderPaymentTransferGroupBy) sqlQuery() *sql.Selector {
	selector := optgb.sql.Select()
	aggregation := make([]string, 0, len(optgb.fns))
	for _, fn := range optgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(optgb.fields)+len(optgb.fns))
		for _, f := range optgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(optgb.fields...)...)
}

// OrderPaymentTransferSelect is the builder for selecting fields of OrderPaymentTransfer entities.
type OrderPaymentTransferSelect struct {
	*OrderPaymentTransferQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (opts *OrderPaymentTransferSelect) Scan(ctx context.Context, v interface{}) error {
	if err := opts.prepareQuery(ctx); err != nil {
		return err
	}
	opts.sql = opts.OrderPaymentTransferQuery.sqlQuery(ctx)
	return opts.sqlScan(ctx, v)
}

func (opts *OrderPaymentTransferSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := opts.sql.Query()
	if err := opts.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (opts *OrderPaymentTransferSelect) Modify(modifiers ...func(s *sql.Selector)) *OrderPaymentTransferSelect {
	opts.modifiers = append(opts.modifiers, modifiers...)
	return opts
}
